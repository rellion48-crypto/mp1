# 미니 프로젝트 1 - 실습 가이드

8/19(수) ~ 8/20(목) · 시니어 자격증 접수 서비스

---

## 0. 만드는 것

화면 **4개**를 만듭니다. DB를 사이에 둔 쌍과 문서를 사이에 둔 쌍입니다.

| 쌍 | 화면 | 하는 일 | 데이터 방향 |
|---|---|---|---|
| DB 쌍 | **1. 접수 화면** | 자격증 고르고 신청서 낸다 | 쓰기 -> DB |
| | **3. 접수 어드민** | 들어온 신청 본다 | 읽기 <- DB |
| 문서 쌍 | **2. FAQ 챗봇** | 문서 보고 답한다 | 읽기 <- 문서 |
| | **4. FAQ 어드민** | 문서를 고친다 | 쓰기 -> 문서 |

**완료 조건**: 공개 주소 3개(접수 사이트 / 챗봇 / ADMIN)가 열린다.

---

## 1. 작업 의존 관계

작업 카드 6장입니다. A가 선행이고, B-C-D와 E-F는 독립입니다. G는 전부 끝난 뒤 합니다.

```
A 자격증 정하기
   |
   +--> B 접수 화면 --> C DB 연결 --> D 접수 어드민
   |
   +--> E 챗봇 --> F FAQ 어드민
   |
   G 배포 (전부 끝난 뒤)
```

**17:00 이후에는 새 기능을 만들지 않습니다.** 17:00부터는 테스트와 기록입니다.

---

## 2. 카드별 할 일과 확인 방법

### A. 자격증 정하기

8종 중 **하나만** 고릅니다.

| 자격증 | 시행기관 |
|---|---|
| 한식조리기능사 / 지게차운전기능사 / 굴착기운전기능사 / 전기기능사 | 한국산업인력공단 (큐넷) |
| 손해평가사 / 공인중개사 | 한국산업인력공단 (전용 사이트) |
| 요양보호사 / 위생사 | 한국보건의료인국가시험원 |

`starter/docs/` 폴더에 자격증별 문서 3개씩 들어 있습니다. 내 자격증 것을 확인하세요.

**확인**: 내 자격증 문서 3개가 `docs/` 폴더에 있다.

### B. 접수 화면

`starter/index.html`을 브라우저로 엽니다. 자격증 카드 8개와 신청 폼이 보입니다.

**확인**: 폼에 이름을 넣고 버튼을 누를 수 있다. Supabase 연결 전이므로 데이터는 저장되지 않는다.

### C. DB 연결 - `script.js` 빈칸 3개

Supabase에서 무료 계정을 만들고 `applications` 테이블을 만듭니다.

| 빈칸 | 위치 | 무엇을 넣나 |
|---|---|---|
| 빈칸 1 | 7-8줄 | Supabase 대시보드 > Settings > API 에서 URL과 anon key 복사 |
| 빈칸 2 | 33줄 | 위에서 선택한 자격증 변수를 연결 |
| 빈칸 3 | 어드민 부분 | 접수 내역을 불러오는 부분 |

**확인**: 폼을 제출하면 Supabase 테이블 화면에 **행이 하나 생긴다.** 새로고침해도 남아 있다.

### D. 접수 어드민

`starter/admin/index.html`을 엽니다.

**확인**: 방금 낸 신청이 표에 보인다.

### E. 챗봇 - `app.py` 빈칸 2개

터미널에서 `python app.py`를 실행합니다.

| 빈칸 | 위치 | 무엇을 넣나 |
|---|---|---|
| 빈칸 1 | 88줄 | 질문 단어 중 문서에서 찾은 것들을 세는 부분 |
| 빈칸 2 | 95줄 | 점수가 이 값보다 낮으면 "모르겠습니다"라고 답할 기준값 |

빈칸 2는 챗봇의 답변 기준값(threshold)입니다. 낮으면 근거 없이 답하고, 높으면 근거가 있어도 거절합니다.

**확인**: `python app.py --test` 를 실행해 5문항이 전부 통과한다.

### F. FAQ 어드민

문서를 화면에서 고칠 수 있게 만듭니다.

**확인**: 문서 하나를 고치면 **챗봇 답이 바뀐다.** 안 바뀌면 파일 경로 또는 캐시 문제다.

### G. 배포

GitHub에 올리고 Vercel로 배포합니다.

**확인**: 주소 3개를 본인 기기 외 다른 기기에서 열어본다.

---

## 3. 8/19 하루 흐름

| 시간 | 무엇 |
|---|---|
| 09:00 - 16:50 | 개발 |
| **17:00 - 17:50** | **테스트 및 버그 수정** |
| **18:00 - 18:50** | **회고 + 수행일지 일차** |

### 17:00 수행 일지 및 테스트 작성 형식

 아래 항목을 채웁니다.

```
 
  수행: 폼에 "홍길동 / 한식조리기능사" 넣고 제출
  기대: Supabase applications 테이블에 행 1개
  실제: 화면이 뜨지 않음
  조치: API 키 변수명이 소문자였음 -> 대문자로 변경
  재실행: 행 1개 생성 확인
  판정: PASS
```

최대한 구체적인 관찰 기록으로 작성합니다.

| 예시 | 구체적인 기록 |
|---|---|
| 터미널에 "성공"이라고 떴다 | 실제 화면을 열어 값이 들어온 것을 봤다 |
| 주소가 만들어졌다 | 그 주소를 다른 기기에서 열어봤다 |
| 일지에 "완료"라고 썼다 | 기대한 결과가 실제로 나왔다 |
| 빨간 글씨가 없었다 | 의도한 동작을 한 번 더 시켜봤다 |

 

---

## 4. 8/20 하루 흐름

| 시간 | 무엇 |
|---|---|
| 09:00 - 13:50 | 확인 -> 수정 -> 완성 |
| **14:00 - 14:50** | **최종 테스트** 및 **사용성 평가** |
| **15:00 - 16:50** | 수정 및 완성 |
| **17:00 - 17:50** | **회고 + 수행일지 완성, PDF 제출** |

### 수정 (오전)

`starter/TUNING_D23.md`를 봅니다. 두 가지를 조정합니다.

- **비슷한말**: "접수비"로 물어도 "응시료" 답이 나오게
- **기준값**: 모르는 질문을 거절하는 문턱

시니어 응시자가 사용하는 용어와 문서 표기가 다릅니다. 비슷한말 사전에 등록해야 검색됩니다.

| 문서에 있는 말 | 어르신이 쓰는 말 |
|---|---|
| 응시료 | 접수비, 시험비, 돈 얼마 |
| 필기 | 1차, 이론, 쓰는 거 |
| 실기 | 2차, 실습, 직접 하는 거 |
| 굴착기 | 포크레인 |
| 요양보호사 | 요양사 |

### 14:00 통합 테스트 3종

| 테스트 | 방법 | 통과 기준 |
|---|---|---|
| 통합 | FAQ 어드민 수정 -> 챗봇 질문 -> 접수 -> 접수 어드민 확인 | 화면 4개가 한 바퀴 돈다 |
| 회귀 | `python app.py --test` 재실행 | 튜닝 전과 같은 5문항 통과 |
| 유저 시나리오 | 아래 3개 | 3개 중 2개 이상 완주 |

**유저 시나리오**

```
1. 접수하러 온 사람
   62세, 요양보호사 시험을 처음 봅니다.
   "응시료가 얼마인지" 챗봇에 묻고, 답을 보고, 접수 폼을 냅니다.
   막히는 화면이 있으면 그 화면 이름을 적습니다.

2. 모르는 걸 묻는 사람
   "시험장에 주차 되나요?" 처럼 문서에 없는 것을 묻습니다.
   챗봇이 지어내지 않고 모른다고 하는지 봅니다.

3. 관리자
   접수 내역을 열어 오늘 몇 명이 신청했는지 셉니다.
```

### 14:00 사용성 평가

옆 사람 서비스를 5분 써보고 3줄 적습니다.

| 항목 | 적을 것 |
|---|---|
| 어디서 멈췄나 | 화면 이름 + 그때 하려던 것 |
| 무슨 말이 어려웠나 | 화면에 있는 단어 그대로 |
| 하나만 고친다면 | 한 문장 |

---

## 5. 막혔을 때

1. `starter/RECOVERY_CARDS.md`를 확인합니다.

---

## 6. 제출물

| 무엇 | 어디에 | 언제 |
|---|---|---|
| 수행일지 PDF (1~2쪽) | **MLP AI 스튜디오 > 과제 제출 > 미니프로젝트(1)** | 8/20 |

 
