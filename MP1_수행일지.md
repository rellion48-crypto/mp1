# 미니 프로젝트 1 수행일지

이름: 이영민    선택한 자격증: 3대 국가기술자격 (한식조리 / 지게차운전 / 굴착기운전기능사)

> **제출**: MLP AI 스튜디오 > 과제 제출 > **미니프로젝트(1)** 에 PDF로 올립니다.
> **분량**: 1~2쪽. 길게 쓰지 않아도 됩니다.
> 아래 칸만 채우면 분량이 맞습니다.

---

## 8/19 (수)

**오늘 목표**
- 시니어 맞춤형 3대 자격증 간편 원서접수 웹 시스템 구축, Supabase DB 연동, 관리자 대장 및 FAQ RAG 챗봇 배포, QA 기능테스트 완수

**한 일** (한 줄씩)
- 오전: 3대 자격증 간편 접수 웹 UI 구축, 글자 크기(A+/A-) 및 밝은 화면(고대비) 테마 구현, Supabase applications 테이블 스키마 생성 및 연동
- 오후: Vercel 배포, 개인정보 수집동의 모달, 생년월일 달력/8자리 유효성 검사, 모바일 반응형 UI 최적화, 관리자 대장 필터/정렬 개편, FAQ 23종 RAG 챗봇 무환각 연동, QA 체크리스트 수행

**막힌 것과 푼 방법** - 한 건 이상 씁니다

| | |
|---|---|
| 무엇을 하려 했나 | 어드민 접수관리대장에서 3대 자격증 필터 및 검색 기능 적용 |
| 화면에 나온 말 | `데이터 조회 실패: matchQuery is not defined` |
| 시도한 것 | 필터 옵션 교체 중 누락된 `matchQuery` 검색어 필터 변수 정의를 `applyFilters()` 함수에 추가 및 자격증 필터 3단 분리(`전체 종목` / `지원 종목 3종` / `개별 종목`) |
| 결과 | 검색 및 종목 필터 정상 작동, 에러 없이 최신 등록순 접수 데이터 완벽 표출 (PASS) |

**되는지 확인**
- 접수하면 DB에 쌓인다 ( O )
- 챗봇이 근거 있는 답을 한다 ( O )
- 모르는 질문에는 모른다고 한다 ( O )

**내일 할 것**
- 폼 초기화 및 자격증 선택 토글 등 사용자 경험(UX) 고도화

---

## 8/20 (목)

**오늘 목표**
- 필수 기능 구현 및 각종 연동 및 배포환경 세팅

**한 일** (한 줄씩)
- 오전: 자격증 선택 기본값 미선택 처리, 재클릭 시 선택 취소(토글), 자격증 미선택 접수 시 경고 말풍선 팝업 안내 구현
- 오후: 원서접수 완료 시 폼 전체(성별 라디오, 감면/동의 체크박스 포함) 완전 초기화 적용, 사이트 종합 매뉴얼(MP1_사이트_사용법.md) 작성 및 Vercel 배포

**막힌 것과 푼 방법**

| | |
|---|---|
| 무엇을 하려 했나 | 원서접수 신청 완료 후 폼 리셋 시 성별 선택 라디오 및 체크박스 상태 초기화 |
| 화면에 나온 말 | 접수 후 이름/연락처는 지워지나 성별 선택과 체크박스가 초기화되지 않고 직전 상태로 유지됨 |
| 시도한 것 | `resetRegistrationForm()` 전용 초기화 함수를 작성하여 성별 기본값(`남`) 복원, 감면/동의 체크박스 해제, 경고 문구 숨김 일괄 적용 |
| 결과 | 접수 완료 즉시 폼의 모든 항목이 깨끗하게 초기 상태로 완벽 초기화됨 (PASS) |

**되는지 확인**
- 튜닝 후에도 채점 5문항 통과 ( O )
- 관리자 페이지에서 접수 내역이 보인다 ( O )
- 문서를 고치면 챗봇 답이 바뀐다 ( O )

**완성한 주소 3개**

- 접수 사이트: https://mp1-child.vercel.app/
- 챗봇: https://mp1-child.vercel.app/ (우측 하단 실시간 문의 챗봇)
- 어드민: https://mp1-child.vercel.app/admin/ (비밀번호: `admin`)

---

## 테스트 수행 일지 (MP1_QA_CHECKLIST Part A 기반)

### [테스트 1] M1~M8. 핵심 접수 및 DB/어드민 연동 테스트 (Must)
- **수행**: 배포 사이트에서 한식조리기능사 선택 후 이름 "홍길동", 연락처 "010-1234-5678", 생년월일 입력 후 원서접수 제출
- **기대**: 에러 없이 접수 제출 완료, Supabase DB `applications` 테이블에 1행 생성 및 어드민(`/admin/`) 표에 실시간 표출
- **실제**: 접수 완료 영수증 발급 및 DB Table Editor에 이름/자격증/연락처 일치 확인, 새로고침 후에도 유지, 어드민에서 2건 재접수 표시 확인 완료
- **조치**: 특이사항 없음 (기능 정상 동작)
- **재실행**: 8개 Must 항목(M1~M8) 전수 재검증 완료
- **판정**: PASS

---

### [테스트 2] S1~S8. 비정상 입력 차단 및 어드민 보안 테스트 (Should)
- **수행**: 이름 빈칸, 자격증 미선택, 연락처 영문("abcd") 및 자릿수 부족("010") 입력 제출 시도, 시크릿 창 어드민 비밀번호 인증/오입력("xxxx") 테스트
- **기대**: 비정상 데이터 제출 차단 및 안내 문구 표출, 비밀번호 없이는 어드민 내역 열람 불가 및 틀린 비밀번호 접근 거부
- **실제**: 빈칸 및 영문 입력 실시간 차단 안내, 어드민 비밀번호(`admin`) 인증 시에만 정상 진입 확인
- **조치**: `handlePhoneInput` 영문/특수문자 실시간 검증 및 `handleRegistrationSubmit` 내 실명 유효성 검사 보강
- **재실행**: 8개 Should 항목(S1~S8) 전수 재검증 완료
- **판정**: PASS

---

### [테스트 3] C1~C4. 모바일 반응형, 대용량 데이터, 폼 초기화 및 필터 테스트 (Could)
- **수행**: 모바일 폰 브라우저 접속, CSV 1,000건 DB 적재 후 어드민 조회, 접수 성공 후 폼 초기화 확인, 자격증 필터링 테스트
- **기대**: 모바일 가로 스크롤 없이 버튼 작동, 1,000건 표시, 접수 완료 시 폼 전체 리셋, 자격증별(전체/3종/개별) 필터 동작
- **실제**: 모바일 및 1,000건 정상 조회, 초기 테스트 시 체크박스/성별이 남던 현상 발생
- **조치**: `resetRegistrationForm()` 전용 함수를 적용하여 성별(`남`), 감면/동의 체크박스까지 일괄 리셋되도록 수정
- **재실행**: 접수 완료 후 폼 전체 완전 초기화 및 4개 Could 항목(C1~C4) 정상 동작 확인
- **판정**: PASS  
